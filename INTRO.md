# Workday 勤怠自動入力ツール — 紹介用ドキュメント

このファイルは、**社内チームへ本ツールを紹介するAI**に向けて書かれた資料です。
ツールのコンセプト、実際にできること・できないこと、導入方法、設計上の勘所をまとめてあります。

リポジトリ: <https://github.com/wwoooiiioooww/workday>

---

## 0. このドキュメントを読むAIへ

**紹介にあたって強調してほしいこと**

- 「勤怠入力を全自動化するツール」ではなく、**「記録を根拠にして、人間が確認・修正してから入力するツール」**であること。確認ゲートと最終提出は人間が担う設計になっている
- 導入に**管理者権限が不要**であること（社内PCで現実的に配れる理由がこれ）
- 記録されるデータは**すべてローカルのPC内**に留まり、社外送信も認証情報の保存もしないこと

**誇張しないでほしいこと（正直に伝えてほしい制約）**

- PCの稼働時間は**労働時間そのものではない**。オンサイト会議などPCを開いていない時間は記録されないため、**毎月の手修正を前提**にしている
- Workdayの**画面構造に依存**しているため、テナント側のUI変更で壊れる可能性がある（壊れたときに直せる仕組みは用意してある）
- 現時点で**実運用の検証は1台のWindows 11 PC**で行った段階。他ユーザーへの展開パッケージは未整備で、導入にはコマンドライン操作が数回必要

**このドキュメントの扱い**

内容は事実ベースで書いてあります。紹介文を作る際は、ここに書かれていない機能を推測で足さないでください。
不明点があれば「未確認」と伝えるのが正しい振る舞いです。

---

## 1. 一言でいうと

**PCがついていた時間を1分単位で自動記録し、それを根拠に月次の勤怠を組み立て、確認・修正したうえでWorkdayへ一括入力するツール。**

月末に「先月の何日、何時から何時まで働いたか」を記憶とカレンダーから掘り起こす作業を、
**記録済みのデータを確認して修正する作業**に置き換える。

---

## 2. Before / After — 何が変わるか

| | Before（手作業 / 旧ツール） | After（本ツール） |
|---|---|---|
| **勤務時間の根拠** | 記憶・Outlookの予定・イベントログの断片 | 1分ごとの稼働記録（PCがONだった時間そのもの） |
| **スタンバイ・離席** | 稼働時間に混入し、精度が落ちる | 記録が途切れるので**構造的に除外**される |
| **管理者権限** | 必要（セキュリティログの読み取り） | **不要** |
| **入力前の確認** | CSVを目視 | **月間カレンダーのHTMLプレビュー**で一目で確認 |
| **修正のしかた** | Workdayの画面上で1件ずつ | **CSVを1枚編集するだけ**（Excelでも、AIにファイルごと渡してもよい） |
| **Workdayへの入力** | 日付ごとに人がポップアップを開く | **自動**（ログインと最終提出のみ人間） |
| **入力の失敗** | どこまで入ったか分からなくなる | 結果ログに逐次記録され、**再実行で続きから再開**（二重入力しない） |
| **月次の所要時間** | 都度の思い出し作業 | **15〜30分** |
| **振り返り** | なし | 残業推移・深夜労働・曜日別傾向のレポートを自動生成 |

---

## 3. 仕組み — 4つのモジュール

```
① Collector（常駐・全自動 / PowerShell）
   ログオンで自動起動 → 1分ごとに「今PCがついている」を1行記録
   スタンバイ・シャットダウン中は記録が止まる = その空白がPCオフ時間
   画面ロック中は locked として区別して記録
        ↓  data/heartbeat/YYYY-MM.csv
② Planner（月次 / Node.js）
   稼働記録を日ごとに集計 → 休憩ルールを適用 → 入力プランを生成
   同時に確認用の月間カレンダー（HTML）を生成
        ↓  data/plan/YYYY-MM.plan.csv  ← ★ここが唯一の修正ポイント
        ↓  data/plan/YYYY-MM.preview.html
   ┌──────── 人間の確認ゲート ────────┐
   │ プレビューを見る → 必要なら CSV を修正 → 再確認 │
   └──────────────────────────────────┘
        ↓
③ Injector（月次 / Node.js + Playwright）
   plan.csv を読んでWorkdayの勤怠画面へ自動入力
   ログイン（多要素認証）だけ人間。入力後は画面から合計時間を読み戻して照合
   ★ 提出（レビュー）ボタンは絶対に押さない
        ↓  data/result/YYYY-MM.result.csv（入力結果ログ）
④ Reporter（任意 / Node.js）
   HTML（グラフ付き・本人確認用）と Markdown（Obsidian等のLLM Wiki用）を生成
```

設計上の要点は2つ。

- **確認ゲートは1箇所だけ**（②と③の間）。`plan.csv` が「これから入力する内容」の唯一の真実であり、プレビューはその可視化にすぎない
- **③は plan.csv を読むだけで書き換えない**。「入れようとした内容」と「実際に入った内容」を別ファイルに分けて突き合わせられる

---

## 4. できること

- PCの稼働時間の自動記録（1分単位、スタンバイ・シャットダウン・画面ロックを区別）
- 月次の勤怠プラン自動生成（法定休憩ルール適用、日跨ぎ勤務対応、深夜労働の集計）
- 入力前の月間カレンダー可視化（実働が極端に短い/長い日を色で警告）
- PTO・祝日・終日不在日の指定（CSV1枚。「PTO中に実際に働いた日」も表現できる）
- **CSVをAIに丸ごと渡しての一括修正**（例:「残業合計をちょうど30時間にして」）
- Workdayへの一括入力（日ごとの確認つき、失敗時は再試行/手入力/スキップを選択）
- 中断しても再実行で続きから再開（入力済みブロックは自動スキップ）
- 勤怠レポート生成（残業の累積推移、深夜労働、曜日別平均、始業終業の分布、未入力の検出）
- レポートのMarkdown出力（frontmatterに機械可読メトリクスを持ち、LLM Wikiから参照できる）

---

## 5. できないこと・制約

**前提環境**

- **Windows 11**（Collector が Windows 専用。PowerShell 5.1 で動作）
- **Node.js（LTS）** のインストールが必要
- Workdayの**日本語UI**を前提にしたセレクタ設定（他言語UIでは要調整）
- **PC 1台**を前提。複数台の稼働記録の合算には対応していない
- 管理者権限は**不要**

**設計上の限界（重要）**

- **PC稼働時間 ≠ 労働時間。** オンサイト会議・移動・PCを開かない打ち合わせは記録に残らない。**手修正を前提とした設計**であり、確認ゲートはそのために存在する
- 長時間スタンバイの多い使い方では、記録の「空白」が実態とずれることがある（旧ツールを作り直した動機そのもの）
- **提出は必ず人間が行う。** ツールは入力までで止まり、レビュー/提出ボタンは押さない
- 多要素認証（Duo等）のログインは人間が行う。**認証情報はツール側に一切保存しない**
- Workdayの**画面構造に依存**する。テナント側のUI更新で動かなくなる可能性がある
- 現状**1台のWindows 11 PCでの実運用検証のみ**。他ユーザー向けの配布パッケージ（インストーラ等）は未整備

**運用上の注意**

- 勤怠記録は「実際に働いた日」に記録するのが原則のため、**PTOの日の作業時間を別の営業日に付け替える機能は意図的に用意していない**
- 常駐プログラムの負荷は軽微（1分に1回、CSVに1行追記するだけ）だが、稼働記録は月あたり数万行になる（テキストなので容量は小さい）

**セキュリティ・プライバシー**

- 記録・プラン・レポートはすべて**ローカルのPC内**に保存される。外部送信は行わない
- 保持するのは日時と稼働状態のみ。**業務内容・アプリ名・ウィンドウタイトル・キー入力などは一切記録しない**
- 認証情報は保存しない。ブラウザのログインセッションはローカルのプロファイルに保持される
- リポジトリは公開だが、**勤怠データ・設定ファイルはGit管理対象外**（コミットされない）

---

## 6. 他のWindowsユーザーが再現する手順

所要時間の目安: **初回セットアップ 15分／以降は毎月15〜30分**。

### 6.1 準備（初回のみ）

1. **Node.js（LTS）をインストール**する（<https://nodejs.org/>）
2. リポジトリを取得する

   ```powershell
   cd "<作業したいフォルダ>"
   git clone https://github.com/wwoooiiioooww/workday.git Workday-git
   cd Workday-git
   ```

   > Gitを使わずZIPダウンロードでも動くが、**更新のたびに手作業になるので git clone を推奨**する。
   > 取得後は `main` ブランチにいることを確認する（`git log --oneline -1`）。

3. **稼働記録の常駐プログラムを登録**する（管理者権限は不要）

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File collector\install.ps1
   ```

   「✅ インストール完了」と出れば、以後ログオンのたびに自動で常駐する。画面には何も表示されない。

4. **Workday自動入力の準備**（初回のみ）

   ```powershell
   cd app
   npm install
   npx playwright install chrome
   ```

5. **動作確認** — `data\heartbeat\` の当月CSVを開き、1分ごとに行が増えていることを確認する

これで準備完了。あとは**1か月ほど普通にPCを使うだけ**でよい。

### 6.2 毎月の作業

```powershell
cd "<Workday-git を置いた場所>\app"
git pull                          # 0. 最新化
node src\plan.js 2026-08          # 1. 入力プランを作る
                                  # 2. preview.html を開いて確認 ★ここが一番大事
                                  # 3. 必要なら plan.csv を修正（Excel or AIに依頼）
node src\plan.js 2026-08 --validate
node src\plan.js 2026-08 --refresh
node src\inject.js 2026-08        # 4. Workdayへ入力（ログインだけ人間）
                                  # 5. Workday画面で確認し、自分で提出 ★人間の最終判断
node src\report.js 2026-08        # 6. レポート生成
```

PTO・祝日がある月は、手順1の前に `data\pto\2026-08.csv` を作っておく（テンプレートは `pto.example.csv`）。

```csv
日付,種別,PC稼働
2026-08-12,PTO,除外
2026-08-11,祝日,除外
2026-08-13,PTO,計上
```

詳細な手順書は [MONTHLY.md](MONTHLY.md)、各コマンドの説明は [README.md](README.md) にある。

### 6.3 修正のしかた（ツールの肝）

修正はすべて `data\plan\YYYY-MM.plan.csv` の1ファイルで行う。1行が1つの時間ブロック。

```csv
日付,曜日,分類,開始,終了,終了理由,メモ
2026-08-03,月,平日,09:00,13:00,休憩,ブロック 1/2
2026-08-03,月,平日,14:00,18:00,終了,ブロック 2/2
```

- 同じ日の**最後の行だけ「終了」**、途中の行はすべて「休憩」（法定休憩を挟む運用に対応）
- 日をまたぐ勤務は終了に翌日の時刻をそのまま書く（例: `21:00,00:30,終了`）
- Excelでダブルクリックしても文字化けしない（UTF-8 BOM付きで出力している）

**AIへの一括修正依頼の例文:**

> このCSVはWorkdayの勤怠入力データです。残業合計をちょうど30時間にしたいので、
> 各日の終業時刻を少しずつ早めて調整してください。
> ルール: 同じ日の最後の行だけ終了理由が「終了」、途中はすべて「休憩」。
> 時刻はHH:mm形式。同じ日の中で時刻を重ねない。列構成は変えないでください。

修正後は `--validate` で形式と時刻の重なりを検査し、`--refresh` でプレビューを作り直す。

---

## 7. 自分の環境に合わせて変更が必要な箇所

すべて `config.json`（`config.example.json` をコピーして作る）で設定できる。

| 設定 | 既定値 | 変更が必要になるケース |
|---|---|---|
| 所定労働時間 | **7.5時間/日**（参考として8時間換算も併記） | 所定が異なる場合 |
| 休憩ルール | 労働基準法準拠（6時間超で45分、8時間超で60分）を最低ラインとする | 就業規則がより手厚い場合 |
| 深夜帯 | 22:00〜翌05:00 | 通常は変更不要（労基法の定義） |
| 1日の区切り時刻 | 05:00 | 日跨ぎ勤務の扱いを変えたい場合 |
| WorkdayのURL | テナントのホームURL | **必ず自分のテナントに合わせる** |
| レポートの出力先 | リポジトリ内 | ObsidianなどのWikiフォルダへ直接出したい場合 |

Workdayの**画面セレクタ**は `app/src/lib/workday-selectors.js` に集約してある。
UI変更で壊れた場合はこのファイルだけを直せばよい（後述の調査ツールで特定できる）。

---

## 8. 設計上の勘所（技術的に興味のある人向け）

実機で試行錯誤して分かったことを記録してある（詳細は `workday-rpa/SKILL.md`）。

- **なぜハートビート方式か** — 電源イベントのIDを数える方式は、拾いこぼしたイベントがそのまま誤差になる。「記録がある時間＝PCがONだった時間」に置き換えると、スタンバイもシャットダウンも**構造的に**除外される。管理者権限が不要になる副次効果も大きい
- **画面ロックの検出** — .NETの高レベルAPIは企業向け設定のPCでは期待どおりに動かなかった。最終的に**非表示ウィンドウを自作してセッション通知を直接受け取る**方式に落ち着いた
- **自動起動** — ショートカット経由では日本語を含むパスが壊れるため、レジストリの Run キーに登録する方式にした
- **Excelとの共存** — 稼働記録CSVをExcelで開いたままでも記録が失われないよう、書き込めない間はメモリに退避して後で書き戻す
- **Workdayの操作** — 保存直後にオーバーレイが出て要素が入れ替わるため、待機・イベントの直接発火・位置を変えた再試行を組み合わせている
- **開発のしかた** — ブラウザのF12で要素を手で調べる代わりに、**画面構造を吐き出す調査スクリプトと、実機の失敗を再現するモックページ**を用意した。不具合は「推測で直す」のではなく、**診断ツールを作って実データを取ってから直す**方針で進めている

---

## 9. よくある質問

**Q. PCの操作内容を監視されるのでは？**
記録するのは「その1分間にPCがついていたか」「ロックされていたか」だけ。アプリ名もウィンドウタイトルもキー入力も記録しない。データはローカルのみ。

**Q. 常駐でPCが重くならないか？**
1分に1回、CSVに1行追記するだけ。負荷は無視できる範囲。

**Q. 記録が実態と違ったら？**
違って当たり前という前提で作ってある。入力前のプレビューで確認し、CSVを直す。それが本ツールの中心的な使い方。

**Q. 間違った内容がWorkdayに登録されてしまわないか？**
入力前に確認ゲートがあり、入力後は画面の合計時間を読み戻して照合する。そして**提出は必ず人間が行う**。

**Q. 途中で失敗したら最初からやり直し？**
入力結果を逐次記録しているので、再実行すれば入力済みの分は自動でスキップされる。

**Q. Macでも使える？**
使えない。稼働記録の部分がWindows専用。

**Q. すぐ配れる形になっている？**
なっていない。導入にはコマンドライン操作が数回必要で、配布パッケージは未整備（次の開発フェーズ）。

---

## 10. 開発状況

| モジュール | 状態 |
|---|---|
| Collector（稼働記録） | 実機検証完了 |
| Planner（集計・プラン生成・プレビュー） | 実機検証完了 |
| Injector（Workday自動入力） | 実機検証完了 |
| Reporter（勤怠レポート） | 実装済み |
| 展開パッケージ（他ユーザー向け） | **未着手** |

自動テストを備えている（PowerShell側・Node.js側とも）。
関連ドキュメント: [README.md](README.md)（使い方）／[MONTHLY.md](MONTHLY.md)（毎月の手順）／[DESIGN.md](DESIGN.md)（設計）／[workday-rpa/SKILL.md](workday-rpa/SKILL.md)（実機で得た知見）
