# Workday勤怠自動入力ツール — 設計書 (v0.2 質問回答反映版)

> ステータス: **承認①待ち**（このドキュメントへの合意が得られるまで実装には着手しない）
> 対象読者: Shota / レビューするAI
> 現行版（`ref/` 配下の3スクリプト）の課題分析を踏まえた新規設計。現行版の踏襲はしない。

---

## 1. 現行版の課題整理（なぜ作り直すか)

| # | 課題 | 原因 | 新設計での対策 |
|---|------|------|----------------|
| 1 | スタンバイ時間が稼働時間に含まれてしまう | イベントログ方式で、スリープ突入/復帰のイベントID（Kernel-Power 42 / Power-Troubleshooter 1）を集計対象にしていない。また「最初と最後のイベントの差」で日次時間を出すため、途中の電源断が見えない | **ハートビート方式**に変更（§3.1）。「記録がある時間＝PCがONだった時間」なので、スタンバイ・シャットダウンは構造的に除外される |
| 2 | 管理者権限が必要 | Securityログ（ロック4800/アンロック4801）の読み取りに管理者権限＋監査ポリシーが必要 | ハートビート方式は通常ユーザー権限で完結。展開時のハードルも下がる |
| 3 | 入力が手間（日付ごとに人間がポップアップを開く） | Workday画面のスクロール等で自動クリックが不安定だったため、人間協業方式に後退した経緯 | Playwrightの自動待機・堅牢なセレクタ（`data-automation-id`）で日付セルのクリックから自動化。失敗時のみ人間にフォールバック（§3.3） |
| 4 | 開発が手間（F12でElements確認→セレクタ手渡し） | Seleniumにはセレクタ記録ツールがない | **Playwright codegen**（操作を録画してセレクタとコードを自動生成）で開発。F12不要（§5） |
| 5 | 入力前の確認が CSV 目視のみ | 可視化の仕組みがない | 入力前に**HTMLプレビュー（カレンダー形式）**を自動生成して確認ゲートにする（§3.2） |

---

## 2. 全体アーキテクチャ

4つの独立したモジュール＋共通データストア。**データは全てプレーンなCSV/JSON**（＝Excelでも、Copilot/Claude等のフロンティアAIでも直接編集できる。コンフィデンシャル情報は含めない）。

```
┌─────────────────────────────────────────────────────────────┐
│ ① Collector（常駐・全自動）            PowerShell            │
│    ログオン時に起動 → 1分ごとにハートビート記録              │
│    出力: data/heartbeat/YYYY-MM.csv                          │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ② Planner（月次・半自動）              Node.js               │
│    ハートビート → 日次稼働時間に集計 → 休憩ルール適用        │
│    → 入力プラン生成 + HTMLプレビュー生成                     │
│    出力: data/plan/YYYY-MM.plan.csv （←ここを自由に編集）    │
│          data/plan/YYYY-MM.preview.html （確認用の可視化）   │
└──────────────────────────┬──────────────────────────────────┘
                           ▼   【人間の確認ゲート】
              plan.csv を編集（Excel / AIに依頼 / そのまま）
              → プレビュー再生成 → OKなら次へ
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ③ Injector（月次・ほぼ自動）           Node.js + Playwright  │
│    plan.csv を読み、Workdayカレンダーに自動入力              │
│    ログイン/Duo認証のみ人間。入力後に画面から実績を読み戻し  │
│    て検証。エラー時は対話的に retry / skip / 手動 を選択     │
│    出力: data/result/YYYY-MM.result.csv （入力結果ログ）     │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ④ Reporter（任意タイミング）           Node.js               │
│    勤怠傾向レポートをHTML生成（残業推移、始業/終業の傾向、   │
│    曜日パターン等）                                          │
│    出力: report/YYYY-MM.report.html                          │
└─────────────────────────────────────────────────────────────┘
```

### データフローの要点

- **確認ゲートは②と③の間の1箇所に集約**。`plan.csv` が「これから入力する内容」の唯一の真実であり、`preview.html` はその可視化。編集手段は問わない（Excel直編集／「残業を30時間ちょうどにして」とAIにファイルごと渡す／後述の対話修正）。
- ③は plan.csv を**読むだけ**（書き換えない）。入力結果は result.csv に分離し、「何を入れようとして、実際何が入ったか」を突き合わせ可能にする。

---

## 3. モジュール詳細設計

### 3.1 Collector — PC稼働時間の記録

**方式: ハートビート（推奨・採用案）**

- ログオン時にスタートアップフォルダ登録（.lnk）で起動する常駐PowerShellプロセス1本。※タスクスケジューラのログオン時トリガーは管理者権限が必要なため不採用（実装時に判明）。
- 1分ごとに `タイムスタンプ, セッション状態(active/locked)` を月次CSVに追記。
- ロック/アンロックは `SystemEvents.SessionSwitch` イベント購読でリアルタイム検知し、ハートビートの状態列に反映。
- **スタンバイ/シャットダウン中はプロセスごと止まる＝記録が途切れる**ので、「1分を大きく超えるギャップ＝PCオフ時間」として集計側で機械的に判定できる。イベントIDの網羅漏れという現行版の失敗モードが原理的に発生しない。

検討した代替案:

| 案 | 内容 | 不採用の理由 |
|----|------|--------------|
| A. イベントログ解析（現行） | ログオン/ロック/スリープ等のIDを網羅して状態機械を組む | ID網羅漏れ・監査ポリシー依存・管理者権限が必要。現行版で実際にスタンバイ除外に失敗している |
| B. 1分ごとにタスクスケジューラでスクリプト起動 | 常駐なし | powershell.exe が毎分起動しコンソールが一瞬光る既知問題。起動オーバーヘッドも無駄 |
| C. ハートビート常駐＋イベントログを補助照合 | AとBのいいとこ取り | v2候補。まずは単純なハートビート単独で精度を実測し、不足があれば追加する（YAGNI） |

補足仕様:
- 日付の区切りは現行踏襲で **AM5:00**（深夜勤務が前日扱いになる）。
- CSVは月次ローテーション。1分×1行でも月間 約3万行/1MB未満で問題なし。
- 多重起動ガード（mutex）を入れる。
- インストールは `install.ps1` 一発（スタートアップ登録のみ。レジストリ・管理者権限不使用）→ 将来の他ユーザー展開が容易。

### 3.2 Planner — 集計・プラン生成・可視化

1. **集計**: heartbeat CSV → 日次の `開始時刻 / 終了時刻 / ロック合計 / 実稼働` に集約。ギャップ（スタンバイ等）は稼働から除外。
2. **休憩ルール適用**（労働基準法準拠をデフォルトとし、パラメータは config.json に外出し）:
   - 6時間以下: 休憩なし / 6時間超: 45分以上 / 8時間超: 60分以上（労基法34条）
   - 長時間勤務時の追加休憩の挟み方（何時間ごとに何分）も config で調整可能にする
   - その日のロック時間合計が規定休憩より長ければロック時間を休憩として採用（現行踏襲）
3. **PTO・終日不在日の扱い**: 月次のプラン生成時に PTO や「オンサイトでPCを開かなかった日」を指定できるようにする（`pto.csv` に日付と種別を書く方式。対話指定も可）。PTO日にPC稼働記録があった場合の扱い（勤怠から除外する／そのまま計上する）は日ごとに選択でき、preview.html 上で PTO日は明示的に色分け表示する。
4. **plan.csv 生成**: 1行=1時間ブロック（日付, 開始, 終了, 終了理由[休憩/終了], 分類[平日/休日/PTO], メモ）。現行の `Workday_Input_*.csv` とほぼ同型だが、終了マーカー「e」のような制御行は廃止（ツール側で完結させる）。
5. **preview.html 生成**: 単一HTMLファイル（依存なし・ブラウザで開くだけ）。
   - 月間カレンダー形式で日ごとの勤務バー・休憩・合計時間・PTO日を表示
   - 月間合計・所定労働時間との差（＝残業時間）・未入力日/異常日（極端に短い/長い日、記録ギャップの多い日）のハイライト
   - 残業時間は所定労働時間 7.5時間/日（config指定）で計算し、参考値として8時間/日換算も併記
   - 「オンサイト会議でPCを開かなかった日」などが一目で分かることが目的
6. **再生成はべき等**: plan.csv を編集したら Planner を `--refresh` で再実行するとプレビューだけ再生成される（集計はやり直さず、編集を上書きしない）。

**AI一括修正との連携**: plan.csv は自己記述的なヘッダ＋README記載のフォーマット仕様を持たせ、「このファイルの残業合計を30時間ちょうどにして」等をそのままAIに渡せる形にする。修正後のファイルは Planner の `--validate` で妥当性検査（時刻の重なり・形式・休憩ルール逸脱の警告）をしてからプレビュー再生成。

### 3.3 Injector — Workdayへの自動入力

- **Playwright（Node.js）+ persistent context**: プロファイルディレクトリを保持することでSSO/Duoのセッションを再利用。初回と期限切れ時のみ人間がログイン。
- フロー:
  1. ブラウザ起動 → Workday時間入力画面へ自動遷移（未ログインなら人間にログインを促して待機）
  2. plan.csv の対象週へカレンダーを自動でナビゲート
  3. 日付セルをクリック → ポップアップに開始/終了/終了理由を入力 → OK →（複数ブロックは繰り返し）
  4. **入力後検証**: カレンダーに表示された当日の合計時間を読み戻し、plan と突き合わせ。不一致なら停止して報告
  5. 全日完了後、月間合計を画面から読み取り最終サマリーを表示。**提出（Submit）は人間**が行う
- **対話的エラー処理**: 各日の入力失敗時にCLIで `[r]etry / [s]kip / [m]anual(手動入力して続行) / [q]uit` を選択。result.csv に全アクションを記録し、途中中断→再開時は入力済みの日をスキップ（べき等）。
- セレクタは Workday の `data-automation-id` を第一優先（UI改版に最も強い）。ラベルテキスト依存は日本語UI前提になるためフォールバック扱い。

**検討した代替案**:

| 案 | 内容 | 判断 |
|----|------|------|
| A. 現行方式（人間がポップアップを開く） | 安定するが月20日×ブロック数のクリックが必要 | 不採用。手間の主因 |
| B. Playwright全自動＋失敗時フォールバック | 推奨 | **採用**。codegenで開発も楽 |
| C. AIエージェント（computer use等）に画面操作させる | UI変化に最強 | 不採用（現時点）。実行のたびに揺らぐ・遅い・展開先ユーザーにAI環境が必要。UI大改版時の**セレクタ再取得の開発手段**としては活用余地あり |

### 3.4 Reporter — 勤怠レポート

- 月次/期間指定でHTMLレポート生成: 残業推移（月間・累計）、始業/終業時刻の分布、曜日別パターン、ロック（中断）時間の傾向、plan と result の差分履歴。
- これも単一HTML・依存なし。データ元は heartbeat / plan / result のCSV群。

---

## 4. 技術スタック（設計判断）

| 判断点 | 選択肢 | 推奨 | 理由 |
|--------|--------|------|------|
| Collector | PowerShell / Python / Node | **PowerShell 5.1（Windows標準）** | 展開先に何もインストールさせない。常駐＋タスク登録はPSが最も自然 |
| Planner/Injector/Reporter | PowerShell / Python+Playwright / **Node.js+Playwright** | **Node.js + Playwright** | codegen・自動待機・trace viewer(失敗時のスクリーンショット付き録画)で開発・デバッグが圧倒的に楽。PSにはPlaywrightがない。PythonでもよいがNodeはPlaywrightが最も一次サポート |
| 配布形態 | zip+セットアップスクリプト | **zip + `install.ps1`**（Node同梱は将来検討） | まずShota環境で完成させる。他ユーザー展開時はNode portable同梱 or 単一exe化（pkg等）をv2で検討 |
| 設定 | ハードコード / config.json | **config.json** | Workday URL・休憩ルール・所定労働時間・日付区切り時刻を外出し。展開時に各自が書き換える箇所を1ファイルに集約 |

## 5. 開発のしやすさ（「F12不要」の実現）

- **Playwright codegen** (`npx playwright codegen <workday-url>`) で実際に画面を操作するだけで、セレクタとコードの雛形が自動生成される。
- 失敗時は **trace viewer** でスクリーンショット付きの実行履歴を後から確認でき、「どこで何が見えていたか」をそのままAIに渡して修正依頼できる。
- Workday画面のセレクタ調査は、Shotaのローカルで codegen を1回実行 → 生成コードをリポジトリに貼るだけ。以後の改修もこの繰り返しで済む。

## 6. リポジトリ構成（案）

```
workday/
├── CLAUDE.md / DESIGN.md / README.md
├── config.example.json          # URL・休憩ルール・所定労働時間など
├── collector/
│   ├── collector.ps1            # 常駐ハートビート
│   ├── install.ps1              # スタートアップ登録＋即時起動＋起動確認
│   └── uninstall.ps1
├── app/                         # Node.js (Planner / Injector / Reporter)
│   ├── package.json
│   ├── src/
│   │   ├── plan.js              # 集計＋plan.csv＋preview.html生成
│   │   ├── inject.js            # Playwright自動入力
│   │   ├── report.js            # レポート生成
│   │   └── lib/                 # 共通(CSV入出力, 休憩ルール, 検証)
│   └── test/                    # 集計・休憩ルール・検証のユニットテスト
├── data/                        # ← .gitignore（個人データはコミットしない）
│   ├── heartbeat/  ├── plan/  └── result/
└── ref/                         # 現行版（参照のみ、変更しない）
```

## 7. 作業順序と段階納品（各フェーズにDoD）

データを貯める側が先（review-core: 段階納品の原則）。**Phase 1 を最優先で納品し、ハートビートを貯め始める**（貯まっていない期間はWorkday入力の元データがないため）。

| Phase | 内容 | DoD（完成の定義） |
|-------|------|-------------------|
| **1. Collector** | ハートビート常駐＋install.ps1 | インストール後、再起動をまたいで heartbeat CSV に1分粒度の記録が残る。スタンバイ→復帰でギャップが正しく記録される（実機で1晩検証）。ロック/アンロックが状態列に反映される。多重起動しない |
| **2. Planner** | 集計＋plan.csv＋preview.html | 実ハートビートから月次plan生成。スタンバイ時間が稼働から除外されている。休憩ルールのユニットテスト全パス。preview.htmlで全日・合計・残業・異常日が確認できる。plan.csv手編集→`--refresh`でプレビューに反映 |
| **3. Injector** | Playwright自動入力 | テスト週で全ブロックが自動入力され、読み戻し検証が一致。エラー時に r/s/m/q が機能。中断→再開で二重入力しない。ログイン以外の人間操作ゼロ（正常系） |
| **4. Reporter** | 勤怠レポート | 指定月のHTMLレポートが生成され、残業推移・始業終業傾向が表示される |
| **5. 展開パッケージ(v2)** | 他Windowsユーザー向け | 別ユーザーの素のWin11で README通りにセットアップ完了できる |

各Phase完了時に成果報告（承認②: Go/NoGo）を行う。Phase 3 は Workday実環境が必要なため、**Shota側での codegen 実行と動作確認の協力**をお願いする形になる。

また CLAUDE.md の方針（自動化・RPA系は着手時に専用Skill新設）に従い、Phase 1 着手時に `workday-rpa` Skill（本プロジェクト固有の知見: Workdayセレクタ規約、ハートビート検証手順、Playwrightデバッグ手順）を新設し、開発中に得た知見を追記していく。

## 8. 確定事項（2026-07-18 Shota回答により確定）

1. **ロック時間の扱い**: 現行踏襲。「ロック中も勤務時間の枠内だが、規定休憩より長ければ休憩に振り替え」。
2. **所定労働時間**: 7.5時間/日（Cisco）を基準に残業計算。参考値として8時間/日換算も併記。会社の祝日カレンダーは考慮しない。**PTOは月次プラン生成時にShotaが指定**し、PTO日のPC稼働の扱いを日ごとに選択できるようにする（§3.2-3）。
3. **PCは1台のみ**: マージ機能は作らない（必要になったらv2で追加）。
4. **Node.js（LTS）インストール可**: Node.js + Playwright 構成で確定。
5. **休憩ルール**: 労働基準法準拠（6時間超45分・8時間超60分）をデフォルトにし、configで調整可能。
6. **Workday UI**: 日本語設定。Duo認証はShotaが対応（ツールはログイン完了を待機するだけ）。

---

*このドキュメントは承認①の対象。承認後、Phase 1 から着手する。*
