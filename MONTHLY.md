# 毎月やること（Shota用チェックリスト）

普段は**何もしなくてよい**。PCの稼働記録は常駐プログラムが自動で取り続けている。
月末〜月初に、以下を上から順にやれば完了する。所要時間の目安は **15〜30分**。

作業フォルダ（以後すべてここで作業する）:

```powershell
cd "<このツールを置いたフォルダ>\app"
# 例: cd "C:\Users\<ユーザー名>\Documents\Workday-git\app"
```

---

## 0. 最新版に更新する（毎回・10秒）

```powershell
git pull
```

---

## 1. 入力プランを作る（1分）

対象月を指定して実行する（例: 2026年8月分）。

```powershell
node src\plan.js 2026-08
```

- `data\plan\2026-08.plan.csv` … Workdayに入力する内容
- `data\plan\2026-08.preview.html` … 確認用のカレンダー

> 2回目以降に作り直したいときは `--force` を付ける（手修正は消えるので注意）。

### PTO・祝日・終日不在の日がある場合

先に `data\pto\2026-08.csv` を作っておくと、プラン生成時に反映される。
フォルダが無ければ `data\pto\` を作る。テンプレートは `pto.example.csv` にある。

```csv
日付,種別,PC稼働
2026-08-12,PTO,除外
2026-08-11,祝日,除外
2026-08-13,PTO,計上
```

- **日付** … `YYYY-MM-DD` 形式（必須）
- **種別** … 自由記述（`PTO` / `祝日` / `半休` など。レポートにそのまま表示される）
- **PC稼働** … その日にPCの記録があったときの扱い
  - `除外` … 入力しない（PCがついていても無視する）。通常はこちら
  - `計上` … PC記録から通常どおり入力を作る（**PTO中に実際に働いた日**などに使う）

> **PTO中に働いてしまった日の扱い**
> PCの記録が残っていると、その日も勤務日として拾われる。どう申告するかは
> 会社のルールに従って自分で決める必要がある。よくある選択肢:
> 1. `除外` にして入力しない（PTOはPTOのまま）
> 2. `計上` にして実際に働いた時間を入力する（PTOの取り下げ・半休化が必要な場合あり）
>
> **勤怠記録は「実際に働いた日」に記録するのが原則**なので、
> 「PTOの日の作業時間を別の営業日に付け替える」ことは推奨しない。
> どうしても必要な場合は plan.csv の日付を手で書き換えれば技術的には可能だが、
> 労働時間の記録としては実態と食い違う点に注意する。

---

## 2. 内容を確認する（5分）★ここが一番大事

`data\plan\2026-08.preview.html` をダブルクリックして開く。

見るポイント:

- 各日の勤務時間が実感と合っているか
- 月間の残業時間が想定内か
- **薄い赤の日**（実働3時間未満／12時間超）が実態と合っているか
- PTO日が正しく色分けされているか

---

## 3. 必要なら修正する（0〜10分）

**`data\plan\2026-08.plan.csv` を直接編集する。** これが唯一の修正ポイント。
Excelでそのまま開ける（文字化けしない）。

### よくある修正パターン

| やりたいこと | 修正方法 |
|---|---|
| **オンサイト会議でPCを開いていなかった日を足したい** | その日の行を追加する（下記の書式）。既にある日なら時刻を伸ばす |
| **残業を減らしたい** | 終業時刻を早める、または開始を遅らせる |
| **残業をちょうど30時間にしたい** | ファイルごとAI（Copilot等）に渡して一括調整を依頼する |
| **この日は入力しない** | その日の行をまるごと削除する |

### plan.csv の書式（1行 = 1つの時間ブロック）

```csv
日付,曜日,分類,開始,終了,終了理由,メモ
2026-08-03,月,平日,09:00,13:00,休憩,ブロック 1/2
2026-08-03,月,平日,14:00,18:00,終了,ブロック 2/2
```

**守るべきルール（守らないと次の手順で止まる）**

- 同じ日の**最後の行だけ「終了」**、途中の行は**すべて「休憩」**
- 時刻は `HH:mm`（`9:00` ではなく `09:00`）
- 同じ日の中で時刻が重ならないこと
- 日をまたぐ勤務は、終了に翌日の時刻をそのまま書く（例: `21:00,00:30,終了`）

### AIに一括修正を頼む場合の例文

> このCSVはWorkdayの勤怠入力データです。残業合計をちょうど30時間にしたいので、
> 各日の終業時刻を少しずつ早めて調整してください。
> ルール: 同じ日の最後の行だけ終了理由が「終了」、途中はすべて「休憩」。
> 時刻はHH:mm形式。同じ日の中で時刻を重ねない。列構成は変えないでください。

### 修正したら必ず確認する

```powershell
node src\plan.js 2026-08 --validate    # 形式・重なりのチェック
node src\plan.js 2026-08 --refresh     # プレビューを作り直す（修正内容は保持される）
```

`--validate` で `✅ 問題は見つかりませんでした。` が出て、プレビューが期待どおりならOK。

---

## 4. Workdayに入力する（5〜10分）

まず**練習（何も登録されない）**をしたい場合:

```powershell
node src\inject.js 2026-08 --dry-run
```

本番:

```powershell
node src\inject.js 2026-08
```

進み方:

1. 入力予定の一覧が出る → 内容を見て `y` + Enter
2. ブラウザが開く → **ログイン（Duo認証）して、勤怠の「週表示」を開く** → PowerShellに戻って Enter
3. あとは自動。日ごとに「入力しますか？」と聞かれる → `y`（`s`でスキップ、`q`で中断）
4. 失敗しても `r`（再試行）/ `m`（手入力した）/ `s`（飛ばす）/ `q`（中断）を選べる

> **中断しても大丈夫。** 再実行すれば入力済みの分は自動で飛ばして続きから再開する。

---

## 5. Workdayで確認して提出する（3分）★人間の最終判断

**ツールは提出（レビュー）ボタンを絶対に押さない。** 自分の目で確認して提出する。

- 各日の時間が正しいか
- 週の合計が想定どおりか

問題なければ Workday の「レビュー」から提出する。

---

## 6. レポートを作る（1分）

```powershell
node src\report.js 2026-08
```

- `data\report\workday-report_2026-08.html` … 自分用（グラフ付き）
- `data\report\workday-report_2026-08.md` … Obsidian用（Copilotが読む）

`config.json` の `report.markdownDir` にObsidianのフォルダを設定しておくと、
Markdownは直接そこへ出力される。

> 一度作ったMarkdownは上書きされない（確定したレポートを守るため）。
> 作り直したいときだけ `--force` を付ける。

---

## 困ったときは

| 症状 | 対処 |
|---|---|
| `plan.csv に修正が必要な点があります` | メッセージの指示どおり plan.csv を直す（休憩/終了の並び順が多い） |
| Workday入力が失敗する | `data\probe\inject-dump-*.txt` が自動生成されるので、それを開発者（AI）に共有する |
| 記録が途切れている | `data\heartbeat\` の当月CSVを見る。常駐が止まっていれば `collector\install.ps1` を再実行 |
| PCを買い替えた／フォルダを移した | `collector\install.ps1` を新しい場所で実行し直す |

## 覚えておくとよいこと

- **修正は必ず plan.csv で行う**（Workdayに入れた後の修正は画面での手作業になる）
- **提出は必ず自分で行う**（ツールは押さない）
- 記録は1分ごとに自動保存されている。CSVをExcelで開いたままでも記録は失われない
